services:
  search:
    restart: unless-stopped

  app:
    environment:
      DB_HOST: ${DB_HOST:-175.0.136.39}
    restart: unless-stopped

  valhalla:
    restart: unless-stopped

  overpass:
    restart: unless-stopped
